import styled from 'styled-components';

export const DeliveryWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 24px;
  position: relative;
  height: 100vh;
  overflow: hidden;
`;
